<% content_for(:title, "Shows on tonight".html_safe) %>

<p id="notice"><%= notice %></p>

<h1>Shows on tonight</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Show</th>
      <th>Episode</th>
    </tr>
  </thead>

  <tbody>
    <% @episodes.each do |episode| %>
      <tr>
        <td><%= link_to episode.show.title, episode.show %></td>
        <td>"<%= link_to episode.title, episode %>" <small>Season <%= episode.season %> Episode <%= episode.number %></small></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Show', new_show_path %>
